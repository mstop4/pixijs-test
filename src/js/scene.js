export class Scene {
  constructor(app) {
    this.app = app;
  }

  init() {
    // Code to initialize scene here
  }

  process(delta) {
    // Code to run every step here
  }
  
  destroy() {
    // Code to destroy scene here
  }
}